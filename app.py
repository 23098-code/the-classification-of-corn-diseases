import streamlit as st
from tensorflow.keras.models import load_model
from tensorflow.keras.preprocessing import image
import numpy as np
from PIL import Image

# =========================
# ตั้งค่าหน้าเว็บ
# =========================
st.set_page_config(
    page_title="Corn Disease Classification",
    page_icon="🌽",
    layout="centered"
)

# =========================
# CSS ธีมสีเขียว
# =========================
st.markdown("""
<style>
.main {
    background-color: #f4fff4;
}
h1, h2, h3 {
    color: #1b5e20;
}
.stButton>button {
    background-color: #2e7d32;
    color: white;
    font-size: 18px;
    border-radius: 8px;
}
.stButton>button:hover {
    background-color: #1b5e20;
}
</style>
""", unsafe_allow_html=True)

# =========================
# หัวข้อ
# =========================
st.title("🌽 ระบบจำแนกโรคใบข้าวโพดด้วย CNN")
st.write("อัปโหลดภาพใบข้าวโพด แล้วกดปุ่ม **วิเคราะห์ภาพ**")

# =========================
# โหลดโมเดล
# =========================
@st.cache_resource
def load_cnn_model():
    return load_model("model.h5")

try:
    model = load_cnn_model()
    st.success("✅ โหลดโมเดลสำเร็จ")
except Exception as e:
    st.error(f"❌ โหลดโมเดลไม่สำเร็จ: {e}")
    st.stop()

# =========================
# ชื่อโรค (อังกฤษ → ไทย)
# ⚠️ ลำดับต้องตรงกับโมเดล
# =========================
class_info = [
    {
        "en": "Corn Blight",
        "th": "โรคใบไหม้ข้าวโพด",
        "care": "ใช้สารป้องกันเชื้อรา ลดความชื้น และตัดใบที่เป็นโรค"
    },
    {
        "en": "Corn Common Rust",
        "th": "โรคราสนิมข้าวโพด",
        "care": "ตัดใบที่ติดเชื้อ และใช้สารป้องกันเชื้อราอย่างเหมาะสม"
    },
    {
        "en": "Corn Gray Leaf Spot",
        "th": "โรคใบจุดสีเทาข้าวโพด",
        "care": "ปลูกพืชหมุนเวียน และเลือกพันธุ์ต้านทานโรค"
    },
    {
        "en": "Healthy",
        "th": "ใบข้าวโพดปกติ (ไม่เป็นโรค)",
        "care": "พืชแข็งแรง ดูแลตามปกติ"
    }
]

# =========================
# อัปโหลดภาพ (รองรับ jfif + webp)
# =========================
uploaded_file = st.file_uploader(
    "📤 อัปโหลดภาพใบข้าวโพด",
    type=["jpg", "jpeg", "png", "jfif", "webp"]
)

if uploaded_file is not None:
    image_pil = Image.open(uploaded_file).convert("RGB")
    st.image(image_pil, caption="ภาพที่อัปโหลด", use_container_width=True)

    if st.button("🔍 วิเคราะห์ภาพ"):
        st.info("⏳ กำลังวิเคราะห์ภาพ...")

        # =========================
        # เตรียมภาพให้ตรงกับโมเดล
        # =========================
        img = image_pil.resize((224, 224))  # แก้ถ้าโมเดลใช้ขนาดอื่น
        img_array = image.img_to_array(img)
        img_array = np.expand_dims(img_array, axis=0)
        img_array = img_array / 255.0

        # =========================
        # ทำนาย
        # =========================
        prediction = model.predict(img_array)
        predicted_class = np.argmax(prediction)
        confidence = float(np.max(prediction))

        disease = class_info[predicted_class]

        st.subheader("📊 ผลการวิเคราะห์")

        # =========================
        # กรองความมั่นใจ
        # =========================
        confidence_threshold = 0.6  # 60%

        if confidence < confidence_threshold:
            st.warning(
                f"⚠️ โมเดลยังไม่มั่นใจในการทำนาย\n\n"
                f"📊 ความมั่นใจ: {confidence*100:.2f}%\n\n"
                "กรุณาใช้ภาพที่ชัดเจน เห็นใบข้าวโพดเต็มใบ"
            )
        else:
            st.success(
                f"🌱 ผลการทำนาย:\n\n"
                f"**{disease['th']}**\n"
                f"({disease['en']})\n\n"
                f"📊 ความมั่นใจของโมเดล: **{confidence*100:.2f}%**"
            )

            st.subheader("🧑‍🌾 แนวทางการดูแลรักษา")
            st.write(disease["care"])
