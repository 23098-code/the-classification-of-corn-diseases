import streamlit as st
import numpy as np
from PIL import Image
from tensorflow.keras.models import load_model

# =========================
# ตั้งค่าหน้าเว็บ
# =========================
st.set_page_config(
    page_title="Corn Disease Classification",
    page_icon="🌽",
    layout="centered"
)

st.title("🌽 ระบบจำแนกโรคใบข้าวโพดด้วย AI")
st.write("อัปโหลดภาพหรือถ่ายภาพใบข้าวโพด เพื่อวิเคราะห์โรค")

# =========================
# โหลดโมเดล
# =========================
@st.cache_resource
def load_cnn_model():
    return load_model("model.h5")

try:
    model = load_cnn_model()
    st.success("✅ โหลดโมเดลสำเร็จ")
except Exception as e:
    st.error(f"❌ โหลดโมเดลไม่สำเร็จ: {e}")
    st.stop()

# =========================
# ข้อมูลคลาส (ต้องตรงกับโมเดล)
# =========================
class_names = [
    "Blight",
    "Common_Rust",
    "Grey_Leaf_Spot",
    "Healthy"
]

class_names_th = {
    "Blight": "โรคใบไหม้ (Blight)",
    "Common_Rust": "โรคราสนิม (Common Rust)",
    "Grey_Leaf_Spot": "โรคใบจุดสีเทา (Grey Leaf Spot)",
    "Healthy": "ใบสุขภาพดี"
}

care_guide = {
    "Blight": """
**🌿 วิธีดูแลโรคใบไหม้**
- ตัดใบที่เป็นโรคออกจากแปลงทันที  
- อย่าทิ้งเศษใบป่วยไว้ในแปลง  
- หลีกเลี่ยงการให้น้ำโดนใบ  
- หากระบาดรุนแรง ใช้สารป้องกันเชื้อรา (เช่น Mancozeb)
""",
    "Common_Rust": """
**🌿 วิธีดูแลโรคราสนิม**
- ตัดใบที่เป็นโรคเพื่อลดการแพร่กระจาย  
- เพิ่มการถ่ายเทอากาศในแปลง  
- ใช้สารป้องกันเชื้อรา (เช่น Propiconazole)
""",
    "Grey_Leaf_Spot": """
**🌿 วิธีดูแลโรคใบจุดสีเทา**
- เก็บและทำลายใบที่เป็นโรค  
- อย่าให้น้ำขังในแปลง  
- ปลูกหมุนเวียน ไม่ปลูกซ้ำที่เดิม
""",
    "Healthy": """
**🌱 ต้นข้าวโพดสุขภาพดี**
- ให้น้ำสม่ำเสมอ ไม่แฉะ  
- ใส่ปุ๋ยตามระยะ  
- หมั่นตรวจใบเป็นประจำ
"""
}

# =========================
# ฟังก์ชันเตรียมภาพ
# =========================
def preprocess_image(image: Image.Image):
    image = image.convert("RGB")
    image = image.resize((224, 224))  # ต้องตรงกับตอนเทรน
    img_array = np.array(image) / 255.0
    img_array = np.expand_dims(img_array, axis=0)
    return img_array

# =========================
# เลือกวิธีรับภาพ
# =========================
st.subheader("📷 เลือกวิธีนำเข้าภาพ")

input_method = st.radio(
    "เลือกวิธี",
    ["📁 อัปโหลดรูป", "📸 ถ่ายภาพจากกล้อง"]
)

image = None

if input_method == "📁 อัปโหลดรูป":
    uploaded_file = st.file_uploader(
        "อัปโหลดภาพใบข้าวโพด",
        type=["jpg", "jpeg", "png", "jfif", "webp"]
    )
    if uploaded_file:
        image = Image.open(uploaded_file)

else:
    camera_image = st.camera_input("กดปุ่มเพื่อถ่ายภาพ")
    if camera_image:
        image = Image.open(camera_image)

# =========================
# แสดงภาพ + ปุ่มวิเคราะห์
# =========================
if image is not None:
    st.image(image, caption="ภาพที่ใช้วิเคราะห์", use_container_width=True)

    if st.button("🔍 วิเคราะห์ภาพ"):
        with st.spinner("กำลังวิเคราะห์..."):
            try:
                img_array = preprocess_image(image)
                prediction = model.predict(img_array)[0]

                predicted_index = np.argmax(prediction)
                confidence = float(prediction[predicted_index])

                # ถ้าความมั่นใจต่ำกว่า 50%
                if confidence < 0.5:
                    st.warning("⚠️ ความมั่นใจต่ำกว่า 50% ไม่สามารถสรุปผลได้")
                    st.info("แนะนำให้ถ่ายภาพใหม่ให้ชัดขึ้น")
                    st.stop()

                predicted_class = class_names[predicted_index]

                st.success(
                    f"✅ ผลการทำนาย: **{class_names_th[predicted_class]}**"
                )
                st.write(f"📊 ความมั่นใจของโมเดล: **{confidence*100:.2f}%**")

                # แสดงวิธีดูแล
                st.subheader("🩺 แนวทางการดูแล")
                st.markdown(care_guide[predicted_class])

            except Exception as e:
                st.error(f"❌ เกิดข้อผิดพลาดในการวิเคราะห์: {e}")
